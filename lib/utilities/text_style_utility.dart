import 'package:flutter/material.dart';

class TextStyleUtility extends StatelessWidget {
  const TextStyleUtility({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
